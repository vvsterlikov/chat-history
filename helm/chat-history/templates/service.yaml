apiVersion: v1
kind: Service
metadata:
  name: {{ include "chat-history.fullname" . }}
  labels:
    {{- include "chat-history.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "chat-history.selectorLabels" . | nindent 4 }}
